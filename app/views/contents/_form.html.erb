
<%= form_for(@content) do |f| %>
  
  <%= f.label :alias %>
  <%= f.text_field :alias %>
  
  <%= f.label :content %>
  <%= f.text_area :content %>
  
  <%= f.submit "Create content", class: "btn btn-large btn-primary" %>

<% end %>
