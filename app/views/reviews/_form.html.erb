<%= form_for(@review) do |f| %>

  <%= f.label :name, 'Имя' %>
  <%= f.text_field :name, :class => 'input-medium' %>
  
  <%= f.label :review, 'Отзыв' %>
  <%= f.text_area :review, rows: 10 %>

  <%= f.submit 'Отправить', :class => 'btn btn-success' %>

<% end %>